import { Area, AreaChart, Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

export function AnalyticsChart({ data, kind = "area", dataKey = "completed" }: { data: Record<string, string | number>[]; kind?: "area" | "bar"; dataKey?: string }) {
  return <div className="chart"><ResponsiveContainer width="100%" height="100%">{kind === "bar" ? <BarChart data={data}><CartesianGrid vertical={false} stroke="var(--chart-grid)" /><XAxis dataKey="name" axisLine={false} tickLine={false} tick={{ fill: "var(--muted-foreground)", fontSize: 11 }} /><YAxis hide /><Tooltip contentStyle={{ background: "var(--popover)", border: "1px solid var(--border)", borderRadius: 6 }} /><Bar dataKey={dataKey} fill="var(--chart-primary)" radius={[3,3,0,0]} /></BarChart> : <AreaChart data={data}><defs><linearGradient id="flowChart" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="var(--chart-primary)" stopOpacity={0.3}/><stop offset="100%" stopColor="var(--chart-primary)" stopOpacity={0}/></linearGradient></defs><CartesianGrid vertical={false} stroke="var(--chart-grid)" /><XAxis dataKey="time" axisLine={false} tickLine={false} tick={{ fill: "var(--muted-foreground)", fontSize: 11 }} /><YAxis hide /><Tooltip contentStyle={{ background: "var(--popover)", border: "1px solid var(--border)", borderRadius: 6 }} /><Area type="monotone" dataKey={dataKey} stroke="var(--chart-primary)" strokeWidth={2} fill="url(#flowChart)" /></AreaChart>}</ResponsiveContainer></div>;
}