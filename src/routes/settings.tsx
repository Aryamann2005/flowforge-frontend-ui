import { createFileRoute } from "@tanstack/react-router";
import { Button } from "../components/Button";
export const Route = createFileRoute("/settings")({ head: () => ({ meta: [
  { title: "Settings — FlowForge" }, { name: "description", content: "Configure FlowForge preferences and API connectivity." }, { property: "og:title", content: "Settings — FlowForge" }, { property: "og:description", content: "Configure FlowForge preferences and connectivity." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" },
] }), component: SettingsPage });
function SettingsPage() { return <div className="page-stack"><section className="page-intro"><div><p className="eyebrow">WORKSPACE</p><h2>Settings</h2><p>Manage your dashboard preferences and API connection.</p></div></section><section className="panel settings-panel"><div className="section-heading"><div><h2>API connection</h2><p>Used by the frontend service when live data is enabled.</p></div></div><label className="field"><span>Base URL</span><input readOnly value="http://localhost:5000/api" /></label><div className="setting-row"><div><strong>Live refresh</strong><p>Automatically update jobs and worker health.</p></div><label className="switch"><input type="checkbox" defaultChecked/><span/></label></div><div className="setting-row"><div><strong>Desktop notifications</strong><p>Alert when a high-priority job fails.</p></div><label className="switch"><input type="checkbox"/><span/></label></div><div className="settings-actions"><Button variant="primary">Save preferences</Button></div></section></div>; }